# ─────────────────────────────────────────────
# AIDoc — Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ─────────────────────────────────────────────

# ── LLM (Required) ──────────────────────────
CEREBRAS_API_KEY=your-cerebras-api-key-here
CEREBRAS_MODEL=llama-3.3-70b

# ── Embedding Model ─────────────────────────
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# ── MongoDB ─────────────────────────────────
# For Docker: leave defaults (auto-configured)
# For Atlas:  mongodb+srv://user:pass@cluster.mongodb.net/?appName=MyApp
MONGO_USER=admin
MONGO_PASS=admin
MONGO_DB=aidoc
MONGO_PORT=27017

# ── GitHub (for private repos) ──────────────
# Generate at: https://github.com/settings/tokens
# Needs "repo" scope for private repositories
GITHUB_TOKEN=

# ── Local Repos (Docker only) ───────────────
# Host directory mounted as /repos inside the container.
# In the UI, use /repos/<folder> to access your projects.
# Example: LOCAL_REPOS_PATH=/Users/me/Desktop
LOCAL_REPOS_PATH=~

# ── App ─────────────────────────────────────
APP_PORT=8501
